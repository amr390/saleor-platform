apiVersion: v1
kind: Service
metadata:
  namespace: saleor-prod
  labels:
    app: dashboard
  name: dashboard
spec:
  type: NodePort
  ports:
  - name: "9000"
    port: 9000
    targetPort: 9000
  selector:
    app: dashboard
status:
  loadBalancer: {}
